spring:
  application:
    name: client-service
  cloud:
    config:
      uri: http://localhost:8888  # Config Server ??
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"
